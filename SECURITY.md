# Security policy

Do not publish GitHub tokens, `.env` files, private keys, SQLite databases,
customer data, logs containing secrets, or build folders.

The launcher stores the ERP read-only token in Windows Credential Manager.
Report security problems privately to the repository owner instead of opening
a public issue with sensitive details.
